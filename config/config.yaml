# Configuration

# Qiita settings
qiita:
  enabled: true
  base_url: "https://qiita.com/api/v2"
  per_page: 20
  # Fetch articles from last N days
  days_back: 1
  # Minimum likes to include
  min_likes: 10
  # Query parameters (optional)
  query: ""

# Zenn settings
zenn:
  enabled: true
  rss_url: "https://zenn.dev/feed"
  max_articles: 50
  days_back: 1
  # Topics to fetch (optional)
  topics: []
  # topics: ["python", "react", "typescript"]

# Claude AI settings
claude:
  model: "claude-sonnet-4-20250514"
  max_tokens: 1000
  temperature: 0.3

# Output settings
output:
  base_dir: "articles"
  filename_format: "{date}_{source}.md"
  date_format: "%Y-%m-%d"

# Logging
logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
